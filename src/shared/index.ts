/**
 * Shared Module - Central exports
 * 
 * Usage:
 * import { Button, useApi, ErrorBoundary } from '@/shared';
 */

// UI Components
export * from './ui';

// Hooks
export * from './hooks';

// Components
export { ErrorBoundary, withErrorBoundary } from './components/ErrorBoundary';
